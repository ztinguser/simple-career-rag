# Career RAG 首页设计 QA

## 对比基准

- 初始页参考图：`C:/Users/mofang/AppData/Local/Temp/codex-clipboard-3e53245a-30ff-45c7-b6e6-8272991168c8.png`
- 聊天页参考图：`C:/Users/mofang/AppData/Local/Temp/codex-clipboard-fcf06af1-5d0d-428e-8761-71ca942991e2.png`
- 页面继续使用原始素材 `background-img.png` 和 `human.png`。
- 本轮目标：简化 Tailwind 类名，并补齐响应式布局。
- 气泡尺寸标注图：`C:/Users/mofang/AppData/Local/Temp/codex-clipboard-fa2acda2-281c-4164-a441-b283fe2531dc.png`。
- 中等宽度布局标注图：`C:/Users/mofang/AppData/Local/Temp/codex-clipboard-e46f207c-a921-421b-a0bf-c0abdc0cb1cf.png`。

## 响应式验证

- 手机端 `390 × 844`：初始页和聊天页均无横向溢出，输入框不超出屏幕。
- 平板端 `768 × 1024`：头像、标题和输入框按 `sm` 断点放大，布局无溢出。
- 桌面端 `1440 × 900`：聊天内容和输入框均受最大宽度限制，不会无限拉伸。
- 长回答可以在消息区域内纵向滚动，底部预留空间，不影响固定输入框操作。

## 交互验证

- 空问题不能发送，超过 500 个字符时显示中文提示。
- 点击发送后立即显示用户气泡和“正在思考...”状态。
- 后端返回后可以显示回答、改写问题和原文引用。
- 手机端与桌面端的真实问答请求均未产生控制台错误。
- 桌面端用户气泡和单行回答气泡约为 `65px` 高，正文为 `20px`，符合本轮缩小要求。

## 浏览器批注修正

- 输入文字固定为 `16px`，字重为 `400`。
- 用户问题、助手回答和引用入口均取消加粗。
- `sm` 断点下输入框由 `48px` 增至 `60px`，约增加四分之一。
- 发送按钮使用黑色圆形填充和白色箭头，禁用状态也保持黑色外观。

## 中等宽度布局修正

- 欢迎页在 `sm` 以上使用视口比例定位头像和标题，不再叠加多组顶部间距。
- `878 × 459` 矮屏下，标题与输入框之间保留约 `100px`，不再重叠。
- 从 `1023px` 切换到 `1024px` 时，输入框只移动约 `12px`，标题位置保持不变。
- `390 × 844`、`878 × 459`、`1440 × 900` 均无横向溢出。

## 输入框预选状态

- 已关闭浏览器自动补全，避免预选问题触发 Chrome 的浅色自动填充背景。
- 输入框在普通、聚焦和自动填充样式下均声明为透明背景。
- 刷新后实测输入问题保持透明，可直接显示外层输入条的蓝色背景。

## 发送按钮间距

- 手机端按钮的上、下、右间距均为 `8px`。
- 平板端按钮的上、下、右间距均为 `10px`。
- 桌面端按钮的上、下、右间距均为 `8px`。

## 问答字号

- 用户问题和助手回答在手机端使用 `14px`。
- `sm` 及以上统一使用 `16px`，平板和桌面不再继续放大。

## 代码检查

- 已删除所有 `clamp()` 和尺寸计算类名。
- 布局统一使用 Tailwind 默认尺寸，以及 `sm`、`lg` 两个响应式断点。
- 唯一保留的任意值是背景图片引用 `bg-[url('/background-img.png')]`。
- `npm run lint` 通过。
- `npm run build` 通过。

## Findings

- 无 P0、P1、P2 问题。
- [P3] 气泡没有参考图中的尖角尾部，目前使用普通异形圆角，避免增加难懂的自定义样式。
- [P3] Figma 字体信息不可用，目前使用系统无衬线字体。

final result: passed
